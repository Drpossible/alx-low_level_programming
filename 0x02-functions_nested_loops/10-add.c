#include "main.h"
/**
 * add - add two integers
 * @a: whole
 * @b: whole
 * Decsription: Function that adds two integers and returns the result
 * Return: a + b
 */
int add(int a, int b)
{
	return (a + b);
}

#include "main.h"

/**
 * _abs - check main
 * @n: An intger input
 * Description: This function returns absolute value of a number
 * Return: Absolute value of number n
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
